function test() {
    echo("hello world");
}

function add(c int, d int) int {
    var e = 3, f = 4;
    return c + d;
}

function main() {
    test();

    var a = 1, b = 2;
    echo(add(a, b + 1));
}
