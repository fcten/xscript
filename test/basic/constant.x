const WELCOME = "Hello" + " " + "World"

echo WELCOME

const MAX_NUMBER = 100 * 100

echo MAX_NUMBER

function test() float {
    const MAX_PRICE = 10.1
    echo MAX_PRICE
    return MAX_PRICE
}

echo test()