const WELCOME = "Hello" + " " + "World";

const MAX_NUMBER = 100 * 100;

function test() float {
    const MAX_PRICE = 10.1;
    print(MAX_PRICE);
    return MAX_PRICE;
}

function main() {
    print(WELCOME);
    print(MAX_NUMBER);
    print(test());
}