const WELCOME = "Hello" + " " + "World";

const MAX_NUMBER = 100 * 100;

func test() float {
    const MAX_PRICE = 10.1;
    echo(MAX_PRICE);
    return MAX_PRICE;
}

func main() {
    echo(WELCOME);
    echo(MAX_NUMBER);
    echo(test());
}