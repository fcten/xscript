const WELCOME = "Hello" + " " + "World";

const MAX_NUMBER = 100 * 100;

function test() float {
    const MAX_PRICE = 10.1;
    echo(MAX_PRICE);
    return MAX_PRICE;
}

function main() {
    echo(WELCOME);
    echo(MAX_NUMBER);
    echo(test());
}